[project]
name = "kafka-tests"
version = "0.1.0"
description = "Kafka producer and consumer tests"
requires-python = ">=3.11"
dependencies = [
    "kafka-python>=2.3.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"